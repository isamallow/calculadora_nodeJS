const soma = (a, b) => { 
    return `Soma: ${a} + {}`
const subtracao = (a, b) => a - b
const multiplicacao = (a, b) => a * b
const divisao = (a, b) => a / b

module.exports = {
    soma,
    subtracao,
    multiplicacao,
    divisao
}